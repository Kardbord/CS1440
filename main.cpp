// Tanner Kvarfordt
// A02052217

#include <cstdlib>

//TODO add documentation
//TODO consider creating company.h or history.h
//TODO a history could represent a simulation over a certain time period rather than just all transactions of an analyst
//TODO add all functions, including getters, to UML diagram
//TODO create table.h that depends on a row.h which depend on cell.h for presentation of data
int main(int argv, char *argc[]) {
    // Keep the main function simple.  It's responsibility should be limited to handle
    // the program argument, creating the initial object(s), stimulating them to do
    // whatever they need to do.

    // For example, think about defining a Comparer class, creating an instance of that
    // class here, and then have it load the analyst data and doing the comparison.
    return EXIT_SUCCESS;
}
